apiVersion: v1
kind: Service
metadata:
  name: {{ include "talkops-sample-app.fullname" . }}
  labels:
    {{- include "talkops-sample-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "talkops-sample-app.selectorLabels" . | nindent 4 }}
