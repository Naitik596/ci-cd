name: ci-cd
'on':
  push:
    branches:
    - main
permissions:
  contents: read
  packages: write
jobs:
  validate-code:
    name: 'Validate: Code Quality'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout
      id: checkout
      uses: actions/checkout@v4
